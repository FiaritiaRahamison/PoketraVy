@page
@model PoketraVy_backoffice.Pages.CategorieUtilisateurBudgets.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategorieUtilisateurBudget[0].IdUtilisateurBudget)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategorieUtilisateurBudget[0].Designation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategorieUtilisateurBudget[0].Categorie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategorieUtilisateurBudget[0].Montant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategorieUtilisateurBudget[0].Daty)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CategorieUtilisateurBudget) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdUtilisateurBudget)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Designation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Categorie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Montant)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Daty)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
